class Address
{
  String placeFormattedAddress;
  String placeName;
  String placeId;
  double longitude;
  double latitude;

  Address({required this.longitude,required this.latitude,required this.placeId,required this.placeName,required this.placeFormattedAddress});
}